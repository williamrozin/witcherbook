import { Button } from '@material-ui/core'
import React, { Component } from 'react'

class Home extends Component {
    public render() {
        return (
            <Button>{ 'Olá' }</Button>
        )
    }
}

export default Home
